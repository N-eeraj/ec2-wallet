import { Injectable } from "@nestjs/common"

@Injectable()
export class AuthService {
  async registerUser() {
    const user = {}
    return user
  }

  async loginUser() {
    const user = {}
    return user
  }

  async logoutUser() {
  }
}
